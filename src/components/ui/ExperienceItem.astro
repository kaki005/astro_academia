---
import type { Experience } from "@/types/cv";
interface Props {
    item: Experience;
    locale?: string;
}
import { locales, getMessages, getTranslations } from "../../i18n";
import TimeDisplay from "@/components/ui/TimeDisplay.astro";
const { item, locale } = Astro.props;
const messages = getMessages(locale);
---

<h2 class="text-2xl font-semibold text-heading my-2">
    {messages.Facurity[item.facurity]}
</h2>
<ol class="text-body mb-4">
    <li>
        {messages.Role[item.role]}
    </li>
    <li>
        <i class="fa-solid fa-location-dot"></i>{
            messages.Location[item.location]
        }
    </li>
    <li>
        <i class="fa-regular fa-calendar"></i>
        <TimeDisplay time={item.start} locale={locale} />
        <text>–</text>
        <TimeDisplay time={item.end} locale={locale} />
    </li>
</ol>
