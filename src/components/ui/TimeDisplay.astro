---
import { locales, getMessages, getTranslations } from "../../i18n";
interface Props {
    locale?: string;
    time?: Date;
}
const { locale, time } = Astro.props;
const { class: className = "" } = Astro.props;
const messages = getMessages(locale);
function formatDate(date: Date, lang?: string) {
    return date.toLocaleDateString(lang === "ja" ? "ja-JP" : "en-US", {
        year: "numeric",
        month: "long",
    });
}
const TIME_CLASS =
    "text-heading text-xs font-medium";
---

{
    time == null ? (
        <time class=`${TIME_CLASS} ${className}`>{messages.common.now}</time>
    ) : (
        <time class=`${TIME_CLASS} ${className}` datetime={time.toISOString()}>
            {formatDate(time, locale)}
        </time>
    )
}
